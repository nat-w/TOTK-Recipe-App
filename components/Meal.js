export default class Meal {
    id;
    name;
    thumbnail;

    constructor (id, name) {
        this.name = name;
        this.effect = effect;
        this.thumbnail = icons.meals[this.name];
    }
}